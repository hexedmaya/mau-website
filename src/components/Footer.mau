<footer class="foot">
  <div class="cols">
    <div>
      <p class="brand">mau<i>.</i></p>
      <p class="tag">Make A UI.</p>
    </div>
    <nav class="nav">
      <a href="#/docs">docs</a>
      <a href="#/docs/start">getting started</a>
      <a href="#/try">try</a>
      <a href="#/about">about</a>
      <a href="#/brand">brand</a>
    </nav>
  </div>
  <div class="line">
    <span>mau, by <b>hexedmaya</b></span>
    <span class="copy">© 2026 hexedmaya</span>
  </div>
</footer>

<style>
  :scope { border-top: 2px solid var(--ink); margin-top: 3rem; font-family: var(--mono); font-size: .85rem; }
  .cols { display: flex; flex-wrap: wrap; justify-content: space-between; gap: 2rem; padding: 2rem clamp(1rem, 4vw, 2.5rem) 1.5rem; }
  .brand { margin: 0; font-family: "Helvetica Neue", Arial, sans-serif; font-size: 3rem; font-weight: 900; letter-spacing: -.06em; line-height: 1; }
  .brand i { color: var(--accent); font-style: normal; }
  .tag { margin: .3rem 0 0; text-transform: uppercase; letter-spacing: .12em; color: var(--mute); }
  .nav { display: flex; flex-direction: column; gap: .3rem; }
  .nav a { color: var(--ink); text-decoration: underline; text-decoration-thickness: 2px; text-underline-offset: .22em; }
  .nav a:hover { background: var(--accent); color: #000; text-decoration: none; }
  .line { display: flex; flex-wrap: wrap; justify-content: space-between; gap: .5rem; padding: 1rem clamp(1rem, 4vw, 2.5rem) 2.5rem; border-top: 2px solid var(--ink); }
  .line b { background: var(--accent); color: #000; padding: 0 .3em; }
  .copy { color: var(--mute); }
</style>
